/*
 * setup.test.ts
 * Placeholder test — proves the Vitest runner and jsdom environment work
 * end to end before any real test suite is written on top of them.
 */
import { describe, expect, it } from 'vitest'

describe('vitest setup', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2)
  })
})
